# Notes

## TODO
- Delete:
  - UserHistory stuff
  - Chat folder in the server
  - Chat slice and service, chat from the redux store

## Future Considerations
- Add a role to the chat so that the question appears as soon as it is asked, not waiting for the answer to generate. So add a role field to the schema that would either be "user" or "chatbot", for example